{
  "name": "css3_scss_project",
  "version": "1.0.0",
  "description": "learning advanced css ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:watch": "sass --watch sass-practice/base.scss:sass-practice/base.css",
    "lite-server": "lite-server",
    "start:dev": "concurrently \"npm run sass:watch\" \"npm run lite-server\""
  },
  "author": "Mukul Kumra",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.4.0"
  },
  "dependencies": {
    "concurrently": "^4.1.0",
    "npm-run-all": "^4.1.5"
  }
}
